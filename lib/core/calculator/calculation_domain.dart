/// Selects whether expressions are evaluated in the real or complex domain.
enum CalculationDomain {
  /// Restricts evaluation to real-valued results.
  real,

  /// Allows complex-valued constants and results.
  complex,
}
