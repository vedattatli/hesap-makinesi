/// Defines how trigonometric input and output values are interpreted.
enum AngleMode {
  /// Degree based calculations, such as `sin(30) = 0.5`.
  degree,

  /// Radian based calculations.
  radian,

  /// Gradian based calculations.
  gradian,
}
